apiVersion: v1
kind: Service
metadata:
  name: openemr-service
  namespace: openemr
spec:
  selector:
    app: openemr
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
