apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql
  namespace: openemr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
      - name: mysql
        image: mariadb:11.4
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: "root"
        - name: MYSQL_USER
          value: "openemr"
        - name: MYSQL_PASSWORD
          value: "openemr"
        - name: MYSQL_DATABASE
          value: "openemr"
        ports:
        - containerPort: 3306
        volumeMounts:
        - name: mysql-data
          mountPath: /var/lib/mysql
      volumes:
      - name: mysql-data
        persistentVolumeClaim:
          claimName: mysql-data
---
apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: openemr
spec:
  selector:
    app: mysql
  ports:
  - port: 3306
    targetPort: 3306
