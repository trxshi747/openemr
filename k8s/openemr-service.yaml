apiVersion: v1
kind: Service
metadata:
  name: openemr
  namespace: openemr
spec:
  selector:
    app: openemr
    version: blue
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
